import React, {FC} from 'react';

const EventsExample :FC = ({}) => {
    return (
        <div>
            <input type="text"/>
            <button></button>
        </div>
    );
};

export default EventsExample;
